Options -Indexes

<FilesMatch "^(config|config\.sample|bootstrap|storage)\.php$">
  Require all denied
</FilesMatch>

<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
</IfModule>
